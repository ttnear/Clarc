### New

- Status-bar context usage and cumulative response time now restore immediately when you reopen a session.
- Rate-limit usage now persists across app restarts, so the status bar no longer starts at `--` after launch.

### Fixed

- Draft text is preserved when navigating message history with the arrow keys.
- Assistant bubbles no longer leave an extra blank line below the final paragraph.
